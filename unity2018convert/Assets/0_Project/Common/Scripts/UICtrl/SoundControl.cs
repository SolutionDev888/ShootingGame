using UnityEngine;
using System.Collections;

public class SoundControl : MonoBehaviour {

	// Use this for initialization
	void Start () {
		LevelSetting.isSound = LevelSetting.isSound;
        LevelSetting.isMusic = LevelSetting.isMusic;
    }
}
